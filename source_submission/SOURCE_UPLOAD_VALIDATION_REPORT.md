# Source-upload validation report

Team DXtra AI - ClaimRoute AI
Datamatics AI Engineering Hackathon 2026

This is an **executed** report, not a checklist. It records what the scans
actually returned. [`SOURCE_UPLOAD_CHECKLIST.md`](SOURCE_UPLOAD_CHECKLIST.md)
remains the gate to run immediately before uploading; this file is the evidence
that the gate has been run once and what it found.

| Field | Value |
|---|---|
| Run date | 2026-08-02 |
| Branch | `docs/final-deliverables` |
| Commit at scan time | `08b7c9b` |
| Tracked files | 264 |
| Upload folder | `DXtraAI_200316_200045_HealthcareAIHackathon` |
| Source archive | `DXtraAI_200316_200045_HealthcareAIHackathon.zip` |
| Upload channel | Organiser SharePoint link, **not** email |

Re-run every scan below against the final upload commit. A report against an
earlier commit does not clear a later one.

---

## 1. Secrets - PASS

| Check | Command | Result |
|---|---|---|
| No tracked secret-like paths | `git ls-files \| Select-String '(?i)(^\.env$\|secret\|credential\|private[_-]?key)'` | **0 hits** |
| No `.streamlit/secrets.toml` | same scan | **absent** |
| `.env` not tracked | same scan | **not tracked** (gitignored) |
| No absolute local paths | `git grep -nI -E "[A-Za-z]:\\\\(Users\|AI-Workspace)" -- ':!*.md'` | **0 hits** |

`.env.example` is tracked and inspected line by line:

| Key | Value | Verdict |
|---|---|---|
| `OPENAI_API_KEY` | *(empty)* | correct |
| `OPENROUTER_API_KEY` | *(empty)* | correct |
| `CLAIMROUTE_MULTIMODAL_ENABLED` | `false` | safe default, not a credential |
| `CLAIMROUTE_LIVE_PROVIDER_TEST` | `false` | safe default, not a credential |
| `CLAIMROUTE_REAL_PROVIDER_SMOKE` | `0` | safe default, not a credential |

Both credential keys are value-free. The three non-empty values are booleans
whose defaults keep spending at exactly zero.

## 2. Organiser data and PHI - PASS

`git ls-files | Select-String '(?i)(diagnostic|overlay|crop|holdout|organiser|organizer)'`
returned four paths. Each is justified:

| Path | Why it is safe |
|---|---|
| `docs/evaluation/official_ub04_holdout.md` | Methodology prose. Aggregate counts only, no record values. |
| `engine/cropper.py` | Source code. The word "crop" is the module's subject. |
| `eval/official/ub04_holdout.py` | Harness code. Reads data it does not contain. |
| `eval/results/official_ub04_holdout_summary.json` | PHI-safe receipt: counts and rates, no field values. |

| Check | Result |
|---|---|
| Organiser datasets tracked | **none** |
| Organiser expected-output files tracked | **none** |
| `eval/official/diagnostics/` tracked | **absent** (gitignored, not force-added) |
| Diagnostic crops, overlays, annotated pages | **none** |
| Raw OCR text from organiser documents | **none** |
| Bundled samples | synthetic only, generated by `data_factory/` |

Protected Tier A holdouts were not accessed. The consumed Tier C holdout was not
rerun.

## 3. Build hygiene - PASS

`git ls-files | Select-String '(?i)(^\.venv/|^venv/|node_modules|__pycache__|\.pyc$|\.pytest_cache|egg-info|vision_cache\.jsonl|03_Demo\.mp4)'`
returned **0 hits**.

No virtualenv, caches, egg-info, vision cache, recording working files, or
superseded draft deliverables are tracked.

## 4. Licences - PASS with one recorded gap

Every non-comment entry in `requirements.txt` appears in the canonical README
licence table:

| Dependency | In README table |
|---|---|
| pillow | yes |
| faker | yes |
| pyyaml | yes |
| numpy | yes |
| pytesseract | yes |
| rapidocr-onnxruntime | yes |
| streamlit | yes |
| pypdfium2 | yes |

**8 of 8 present. The scored rule is satisfied.**

Carried forward unchanged from
[`LICENSES_AND_DEPENDENCIES.md`](LICENSES_AND_DEPENDENCIES.md): `pypdfium2` is in
the canonical README table but missing from `docs/licensing.md`. The scored rule
is met; the secondary audit lags by one row. Not fixed here - `docs/licensing.md`
is an evidence document outside this task's scope.

Tesseract 5.4.0 (Apache-2.0) is an external binary, installed separately and
resolved via PATH. It must not be bundled.

## 5. Artifact-generation dependencies - PASS

The submission generators need two packages that the **runtime does not**:

```
reportlab==5.0.0
openpyxl==3.1.5
```

Install them only when rebuilding deliverables:

```powershell
.\.venv\Scripts\python.exe -m pip install reportlab==5.0.0 openpyxl==3.1.5
```

| Check | Result |
|---|---|
| `reportlab` in `requirements.txt` | **absent** - correct |
| `openpyxl` in `requirements.txt` | **absent** - correct |

They are deliberately **excluded from runtime `requirements.txt`**. The
extraction pipeline, the evaluation harness, and the Streamlit app all run
without them; only `scripts/submission/` imports them. `requirements.txt` was not
modified by this work.

## 6. Docker - MISSING (declared, not fabricated)

`git ls-files` shows no `Dockerfile`, no `docker-compose.yml`, no
`.dockerignore`. None was created.

Docker is **preferred, not required** by the organiser. Docker is not installed
on the build machine, so no image could be built or validated. Shipping an
unvalidated Dockerfile would be worse than a documented absence: it would fail on
the reviewer's machine and imply a deployment path that was never tested.

**Status: MISSING. Stated honestly in the README and the architecture PDF. Do not
create a placeholder Dockerfile to close this row.**

## 7. Exclusions from the source package

The upload must exclude, per
[`EXCLUSION_MANIFEST.md`](EXCLUSION_MANIFEST.md):

`.git/` · `.venv/` · `.env` · credentials and API keys · caches ·
`submission/final/` · the demo MP4 · the final email ZIP · organiser documents ·
`Images & Output/` · expected-output files · PHI · diagnostic crops · raw OCR
exports · raw provider responses · `tmp/` · local-only files · internal agent or
tool configuration not needed by reviewers.

Most are already gitignored, so an archive built from a clean `git archive`
excludes them by construction. Verify anyway: gitignore protects the repository,
not a hand-built ZIP.

## 8. Known test failures - documented, not hidden

Recorded in [`SOURCE_UPLOAD_CHECKLIST.md`](SOURCE_UPLOAD_CHECKLIST.md) and
repeated here so the upload does not ship a silent surprise: two freeze-manifest
hash tests fail on Windows checkouts with `core.autocrlf=true`, because CRLF
conversion changes the bytes of pinned files before hashing. Pre-existing,
reproducible on the parent commit, and not caused by any submission work.

Fix with a `.gitattributes` entry on a separate branch, or document explicitly.
**Do not weaken the test to make it pass.**

## 9. Not verified by this report

Stated so the gaps are not mistaken for passes:

- The upload archive itself has not been built, so its contents are unverified.
- No clean-checkout build was performed on a second machine.
- `docs/licensing.md` was not corrected.
- Frame-level inspection of `03_Demo.mp4` is impossible: it does not exist yet.

## Verdict

**Source-upload readiness: READY, pending the checklist re-run against the final
upload commit.**

Every scan that can be run before the archive exists has been run and passed.
Docker remains a declared MISSING, which is a deliberate honest position rather
than an open defect.
